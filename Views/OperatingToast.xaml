<Window x:Class="EveCommandCenter.Views.OperatingToast"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Width="370" Height="132" WindowStyle="None" ResizeMode="NoResize"
 AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False"
 ShowActivated="False" Topmost="True" FontFamily="Segoe UI">
 <Border Background="#F207181B" BorderBrush="#55D7D2" BorderThickness="1" CornerRadius="8" Padding="14" MouseLeftButtonUp="Open_Click" Cursor="Hand">
  <Grid>
   <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
   <TextBlock Text="MOON ALERT" Foreground="#FFD166" FontSize="11" FontWeight="Bold"/>
   <Button Content="×" HorizontalAlignment="Right" Width="24" Height="22" Margin="0,-6,-6,0" Background="Transparent" BorderThickness="0" Foreground="#8FB2B5" Click="Dismiss_Click" ToolTip="Dismiss notification"/>
   <StackPanel Grid.Row="1" Margin="0,8,0,4">
    <TextBlock x:Name="StructureText" Foreground="#EAF7F7" FontWeight="SemiBold" TextTrimming="CharacterEllipsis"/>
    <TextBlock x:Name="MessageText" Foreground="#8FB2B5" FontSize="12" TextWrapping="Wrap" Margin="0,5,0,0"/>
   </StackPanel>
   <TextBlock Grid.Row="2" Text="EVE COMMAND CENTER  ·  Click to inspect" FontSize="9" Foreground="#52777B"/>
  </Grid>
 </Border>
</Window>
