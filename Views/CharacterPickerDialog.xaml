<Window x:Class="EveCommandCenter.Views.CharacterPickerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{DynamicResource L.Pick.Title}"
        Width="360" Height="440"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BgDarkBrush}"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock x:Name="TxtTitle" Grid.Row="0" FontSize="14" FontWeight="SemiBold"
                   Foreground="{StaticResource AccentBrush}" Margin="0,0,0,8"/>

        <!-- Search / Filter -->
        <TextBox x:Name="TxtSearch" Grid.Row="1"
                 Background="{StaticResource BgPanelBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 Padding="6,4" FontSize="12"
                 TextChanged="OnSearchChanged">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Resources>
                        <VisualBrush x:Key="HintBrush" TileMode="None" Opacity="0.5" Stretch="None" AlignmentX="Left">
                            <VisualBrush.Visual>
                                <TextBlock Text="{DynamicResource L.Pick.SearchHint}" FontStyle="Italic" Foreground="Gray" Margin="6,4,0,0"/>
                            </VisualBrush.Visual>
                        </VisualBrush>
                    </Style.Resources>
                </Style>
            </TextBox.Style>
        </TextBox>

        <!-- Scrollable character list -->
        <ListBox x:Name="LstCharacters" Grid.Row="2" Margin="0,8,0,8"
                 Background="{StaticResource BgPanelBrush}"
                 Foreground="{StaticResource TextPrimaryBrush}"
                 BorderBrush="{StaticResource BorderBrush}"
                 FontSize="12"
                 MouseDoubleClick="OnListDoubleClick"
                 SelectionChanged="OnSelectionChanged"/>

        <!-- Manual entry label -->
        <TextBlock Grid.Row="3" Text="{DynamicResource L.Pick.ManualHint}"
                   Foreground="{StaticResource TextSecondaryBrush}" FontSize="10" Margin="0,0,0,8"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="{DynamicResource L.Common.Ok}" Width="80" Padding="0,5" Click="OnOK" IsDefault="True"
                    Background="{StaticResource AccentBrush}" Foreground="White"
                    BorderThickness="0" FontWeight="SemiBold" Margin="0,0,8,0"/>
            <Button Content="{DynamicResource L.Common.Cancel}" Width="80" Padding="0,5" Click="OnCancel" IsCancel="True"
                    Background="Transparent" Foreground="{StaticResource TextPrimaryBrush}"
                    BorderBrush="{StaticResource BorderBrush}"/>
        </StackPanel>
    </Grid>
</Window>
