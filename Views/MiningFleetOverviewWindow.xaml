<Window x:Class="EveMultiPreview.Views.MiningFleetOverviewWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mining Overview"
        Width="1500" Height="150" MinWidth="620" MinHeight="130"
        WindowStyle="None" ResizeMode="CanResizeWithGrip"
        ShowInTaskbar="False" Topmost="True"
        Background="Transparent" AllowsTransparency="True">
    <Window.Resources>
        <SolidColorBrush x:Key="TealBrush" Color="#39C6A4"/>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#26333A"/>
            <Setter Property="Foreground" Value="#EAF7F4"/>
            <Setter Property="BorderBrush" Value="#39796C"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Border Background="#F20E1518" BorderBrush="#39796C" BorderThickness="1"
            CornerRadius="8" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <DockPanel Grid.Row="0" Margin="0,0,0,7"
                       MouseLeftButtonDown="Header_MouseLeftButtonDown">
                <TextBlock Text="MINER OVERVIEW · LIVE YIELD"
                           Foreground="#EAF7F4" FontWeight="SemiBold" FontSize="13"/>
                <Button DockPanel.Dock="Right" Content="×" Width="27" Height="24"
                        Click="Close_Click" Margin="8,0,0,0"/>
                <TextBlock x:Name="UpdatedText" DockPanel.Dock="Right"
                           Foreground="#7F9B96" VerticalAlignment="Center"/>
            </DockPanel>

            <ScrollViewer Grid.Row="1" HorizontalScrollBarVisibility="Auto"
                          VerticalScrollBarVisibility="Disabled">
                <ItemsControl x:Name="MinerItems">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="8" BorderThickness="2" Padding="10,7"
                                    Margin="0,0,8,0" MinWidth="148" Background="#151D20">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="BorderBrush" Value="#39796C"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="MINING">
                                                <Setter Property="BorderBrush" Value="#3EBF91"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="LATE">
                                                <Setter Property="BorderBrush" Value="#D7A84B"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="DEGRADED">
                                                <Setter Property="BorderBrush" Value="#E38B3E"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="IDLE ⚠">
                                                <Setter Property="BorderBrush" Value="#E05252"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <StackPanel>
                                    <TextBlock Text="{Binding Character}" Foreground="#EAF7F4"
                                               FontWeight="SemiBold" FontSize="12"/>
                                    <TextBlock Text="{Binding BaseText}" Foreground="#FFFFFF"
                                               FontWeight="Bold" FontSize="18" Margin="0,3,0,0"/>
                                    <TextBlock Text="{Binding ActualText}" Foreground="#8FA8A3"
                                               FontSize="11" Margin="0,1,0,0"/>
                                    <TextBlock Text="{Binding Status}" FontSize="11" Margin="0,2,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#3EBF91"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Status}" Value="LATE">
                                                        <Setter Property="Foreground" Value="#D7A84B"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="DEGRADED">
                                                        <Setter Property="Foreground" Value="#E38B3E"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="IDLE ⚠">
                                                        <Setter Property="Foreground" Value="#E05252"/>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Status}" Value="WAITING">
                                                        <Setter Property="Foreground" Value="#77817F"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
