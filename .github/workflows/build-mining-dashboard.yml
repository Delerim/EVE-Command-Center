name: Build EVE Command Center

on:
  workflow_dispatch:
  push:
    branches: [ main, mining-dashboard, esi-sso ]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'

      - name: Restore
        run: dotnet restore EveMultiPreview.csproj

      - name: Publish EVE Command Center EXE
        run: dotnet publish EveMultiPreview.csproj -c Release -r win-x64 --self-contained true /p:PublishSingleFile=true /p:IncludeNativeLibrariesForSelfExtract=true -o artifacts/publish

      - name: Upload EVE Command Center artifact
        uses: actions/upload-artifact@v4
        with:
          name: EVE-Command-Center-Windows-x64
          path: artifacts/publish/**
          if-no-files-found: error
